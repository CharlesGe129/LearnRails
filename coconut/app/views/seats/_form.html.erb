<%= form_with(model: seat, local: true) do |form| %>
  <% if seat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seat.errors.count, "error") %> prohibited this seat from being saved:</h2>

      <ul>
      <% seat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :flight_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :seat_name %>
  </div>

  <div class="field">
    <%= form.label :baggage %>
    <%= form.text_field :baggage, id: :seat_baggage %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
